# Tourism-project
Taking messy, scattered tourism data, cleaning and joining it into one dataset, exploring it, building 3 models (predict rating, predict visit mode, recommend attractions), evaluating them, and packaging everything into a Streamlit app + report.
